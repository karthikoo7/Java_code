package hashSet;

import java.util.Objects;

public class Students {
	
	int sid;
	String sname;
	String city;
	float percent;
	
	public Students() {
		// TODO Auto-generated constructor stub
	}
	public Students(int sid, String sname, String city, float percent) {
		
		this.sid = sid;
		this.sname = sname;
		this.city = city;
		this.percent = percent;
	}
	
	public String toString() {
		return "Students [sid=" + sid + ", sname=" + sname + ", city=" + city + ", percent=" + percent + "]";
	}
	
	@Override
	public int hashCode() { //for grouping i.e bucket criteria
		//return Objects.hash(city, percent, sid, sname);
		
		int code = 0;
		if(city.equals("pune")) {
			code = 10;
		}
		else if(city.equals("mumbai")) {
			code = 20;
		}
		else if(city.equals("nashik")) {
			code = 40;
		}
		return code;
	}
	
	public boolean equals(Object obj) {
		boolean flag = false;
		
		if(obj instanceof Students) {
			Students s = (Students)obj;
			if(this.sid == s.sid && this.sname.equals(s.sname) && this.city.equals(s.city) && this.percent == s.percent);
		}
		
	
	
		return flag;
	}
	
	


}
